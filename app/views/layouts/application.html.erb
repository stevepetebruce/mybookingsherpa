<!DOCTYPE html>
<html>
  <head>
    <link rel="canonical" href="https://mybookingsherpa.com"/>
    <% if Rails.env.production? %>
      <%= render "layouts/vendor_code/google_tag_manager_head" %>
    <% end %>
    <title>MyBookingSherpa</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_pack_tag "application", "data-turbolinks-track": "reload" %>
    <%= stylesheet_pack_tag "application", "data-turbolinks-track": "reload" %>
    <script src="https://js.stripe.com/v3/"></script>
    <% if Rails.env.production? %>
      <%= render "layouts/vendor_code/drift" %>
    <% end %>
  </head>

  <body class="header-fixed">
    <% if Rails.env.production? %>
      <%= render "layouts/vendor_code/google_tag_manager_body" %>
    <% end %>
    <div class="page-wrapper">
      <%= render "layouts/nav_bar" %>
      <% if @current_guide&.on_trial? %>
        <%= render partial: "layouts/in_trial_banner", locals: { extra_top_margin: true } %>
      <% end %>
      <%= flash_messages %>
      <%= yield %>
    </div>
  </body>
</html>

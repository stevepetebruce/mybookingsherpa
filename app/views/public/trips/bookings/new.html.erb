  <div class="container-fluid full-height">
    <div class="row row-height">
      <div class="col-lg-4 content-left" style="">
        <div class="content-left-wrapper bg_img">
          <div class="wrapper">
            <div class="py-4 text-center">
              <% if @booking.organisation.logo_image.attached? %>
                <%= image_tag url_for(@booking.organisation_logo_image), class: "d-block mx-auto mb-4 logo" %>
              <% end %>
              <h1 class="font-weight-bold mb-0"><%= @booking.trip_name %></h1>
              <h3>
                <%= "#{@booking.start_date.day} - #{@booking.end_date.day} #{Date::MONTHNAMES[@booking.end_date.month]}" %>
              </h3>
              <% if @booking.only_paying_deposit? %>
                <h1 class="mb-0">Deposit: <%= @booking.human_readable_amount_due %></h1>
                <span class="mb-5">(Due now)</span>
              <% end %>
              <h1 class="<%= 'mb-0' if @booking.only_paying_deposit? %>">Total: <%= @booking.human_readable_full_cost %></h1>
              <% if @booking.only_paying_deposit? %>
                <span class="mb-3">(Full payment will be taken on <%= @booking.human_readable_full_payment_date %>)</span>
              <% end %>
              <div id="summary" class="mt-5">
                <p class="collapse" id="collapseSummary">
                  <%= @booking.trip_description %>
                </p>
                <a class="collapsed" data-toggle="collapse" href="#collapseSummary" aria-expanded="false" aria-controls="collapseSummary"></a>
              </div>
            </div>
            <h4 class="d-flex justify-content-between align-items-center mb-3">
              <!-- TODO: do we really need this info (below) ? -->
              <!-- <span>Places left for this trip</span> -->
              <!-- <span class="badge badge-secondary badge-pill"> -->
                <!-- TODO: need to show a warning when there's no spaces left on the trip -->
                <% # @booking.trip_maximum_number_of_guests - @booking.trip_guest_count %>
              <!-- </span> -->
            </h4>
          </div>
        </div>
      </div>
      <div class="col-lg-7 content-right">
        <% # TODO: this will post to Stripe API - remove this form when it does %>
        <%= render 'new_form', booking: @booking, url: public_trip_bookings_path(@trip.slug) %>
      </div>
    </div>
  </div>
</div>

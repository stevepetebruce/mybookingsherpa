<!-- 
Do we need a confirmation step - just before payment?
ex: a modal with the summary payment to be made? 
An extra step - but a definite confirmation from the user.
-->

<%= form_with(model: booking, local: true, url: public_trip_bookings_path(@trip.slug), class: "needs-validation", html: { id: "payment-form", novalidate: :novalidate }, method: :post, data: { controller: "country-auto-select" }) do |form| %>
  <% # reinstate error / warning notices %>
  <!-- <div class="alert alert-warning" role="alert"><%= notice %></div>
  <div class="alert alert-warning" role="alert"><%= alert %></div> -->
  <h4 class="mb-3 font-weight-bold">Your Details</h4>
  <% if booking.organisation_on_trial? %>
    <p style="color: red;">
      <small>Psst...</small>
      <br/>
      <small>As you're in trial, we've filled the form with pretend data, so you can see what an example booking looks like.</small>
      <br/>
      <small>When you complete the sign up process, you can start putting real bookings and money through here 😀.</small>
    </p>
  <% end %>
  <div class="row">
    <div class="col-md-12 mb-3">
      <%= form.label :name, "Full Name" %>
      <%= form.text_field :name, value: trial_example_full_name_or_nil(booking), class: "#{ 'is-invalid' if @booking.errors[:name].present?} form-control", autofocus: auto_focus(booking, form_field: :name, default_auto_focus: true), required: true, readonly: booking.organisation_on_trial? %>
      <div class="invalid-feedback">
        <%= "Please enter your name" %>
      </div>
      <% if @booking.errors[:name].present? %>
      <div class="invalid-feedback" style="display: block;">
        Please enter a valid name
      </div>
    <% end %>
    </div>
  </div>
  <div class="mb-3">
    <%= form.label :email %>
    <%= form.text_field :email, value: trial_example_email_or_nil(booking), class: "#{ 'is-invalid' if @booking.errors[:email].present?} form-control", autofocus: auto_focus(booking, form_field: :email), required: true, readonly: booking.organisation_on_trial? %>
    <div class="invalid-feedback">
      Please enter a valid email
    </div>
    <% if @booking.errors[:email].present? %>
      <div class="invalid-feedback" style="display: block;">
        Please enter a valid email
      </div>
    <% end %>
  </div>
  <hr class="mb-5 mb-4">
  <h4 class="mb-3 font-weight-bold">
    <% if @booking.only_paying_deposit? %>Deposit <% end %>Payment of <%= @booking.human_readable_amount_due %>
    </h4>
  <div class="row">
    <div class="col-md-12 mb-3">
      <% if booking.organisation_on_trial? %>
        <span>Credit or debit card number</span> <span style="padding-left: 265px;">MM / YY</span> <span style="padding-left: 18px;">CVC</span>
        <%= text_field_tag :fake_card_details, "4242 4242 4242 4242                                                          #{1.year.from_now.strftime("%m / %y")}    123", class: "form-control", required: true, readonly: true %>
        <small style="color: red;" class="text-center">
          This is just a pretend card number. Don't worry, Emmanuel isn't really going to get charged.
        </small>
      <% else %>
        <label for="card-element">Credit or debit card</label>
        <div id="card-element">
          <!-- A Stripe Element will be inserted here. -->
        </div>
        <!-- Used to display Element errors. -->
        <div id="card-errors" role="alert"></div>
      <% end %>
    </div>
  </div>
  <%= hidden_field_tag :country_code, nil, data: { target: "country-auto-select.countrySelectField" } %>
  <%= hidden_field_tag :int_phone_code, nil, data: { target: "country-auto-select.phoneField" } %>
  <div class="actions">
    <%= form.submit "Book My Place", class: "btn btn-primary btn-lg btn-block mt-4 mb-4" %>
  </div>
  <% if booking.organisation_on_trial? %>
    <p style="color: red;" class="text-center">
      <small> Now hit that big button ☝️ and see what happens next...</small>
    </p>
  <% end %>
<% end %>